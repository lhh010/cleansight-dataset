# CleanSight Benchmark 等价类划分 · 时序动作分割（cleansight-ActionMixed）

> 适用数据集：**cleansight-ActionMixed**（检测 bbox + 动作标签，逐帧判 phase 把视频切成动作段）
> 目的：为 benchmark **test 集人工策展**提供等价类依据（不是训练集划分）
> 前身：由 `DATASET_TEST_SCENARIOS.md` 的动作相关维度重排而来，按"交互/运动为主轴"的结论重构
> 配套：`BENCHMARK_DETECTION.md`（检测那一套，独立维护）

---

## 0. 前提与核心命题

- **测试单元**：**整条序列**（不可截散段）。分割指标必须在完整序列上算。
- **主指标**：frame-wise accuracy、**edit distance**、**F1@{10,25,50}**、**边界定位误差（容忍窗 τ）**、段数比（预测段/真值段）。头号失败模式 = **过分割（over-segmentation）**。
- **核心命题（决定整套 EC）**：
  > bbox 只表示"该器具**可见**"，而清洗台常有杂物同框——**"看到什么器具"推不出"在做什么动作"**。
  > 动作的判别信号是 **① 手—器具的交互关系** + **② 手本身的运动模式** + **③ 时序动态**，可见性只是**线索**，不是判据。
- **推论**：benchmark 必须专门包含**"仅凭可见器具会判错"的对抗序列**，否则模型可能只学到"看见气枪→报注气"的捷径，却在 benchmark 拿高分（失真）。
- **泄漏规则**：test 视频**源级整条隔离**，绝不进 train/val——分割模型对同场次的转移先验尤其敏感，会背下你的动作转移概率。
- **优先级定义**：P0 必测核心+最大缺口 / P1 重要难点 / P2·基线 补充或已饱和。
- **数据现状**依据 `DATASET_STATUS.md`（ActionSequence 帧数）、`config.yaml`。

---

## 1. 评测能力边界（已知盲区）

> benchmark 是尺子，必须诚实声明"量了什么、没量什么"。**绿灯不代表跨域泛化。**

**v0.1 覆盖**：**域内**（同内镜型号 / 同操作者 / 同场景）的时序分割表现 + **unseen clip**（未见片段/背景）。

**泛化轴现状：**

| 泛化轴 | v0.1 状态 | 说明 |
|---|---|---|
| 域内（同型号/操作者/场景） | ✅ 评测 | 评测主体 |
| unseen clip（整源留出） | ✅ 评测 | 视频级留出整条源视频作 test；**非跨域**，抓的是过拟合到特定场次的转移先验/背景 |
| 异机位 / 角度 | 🟡 条件 | 若现有场景能换角度重录一小段则补为 P1；否则并入盲区 |
| 异内镜型号 | ⛔ **盲区** | 无数据源，v0.1 **不评测**，列入 roadmap |
| 异操作者 | ⛔ **盲区** | 无数据源，v0.1 **不评测**，列入 roadmap |

> 跨型号/操作者泛化是已知盲区。时序分割尤其危险的一点：模型会**背下同场次的动作转移概率**——unseen clip 若与 train 同场次，衰减会被低估。选 test 源时尽量取**不同录制场次**以逼近真实泛化。待数据可得后补 `e2e_test` 单独评。

---

## 2. 等价类维度

### Group A — 交互 / 运动语义（**主轴**，可见性降为线索）

这是本套最核心、也是最缺的对抗维度：判别动作靠交互与运动，而非器具是否在画面。

| 等价类 | 说明 | 性质 | 现状 | 来源 | 优先级 |
|---|---|---|---|---|---|
| 器具**在用**（手抓握 + 操作） | 正常正样本 | V | ✅ | 派生（手-器具几何+共动） | 基线 |
| **器具在场但未操作**（杂物 / 待命 / 放下） | 看到气枪 ≠ 在注气；不能被"可见"骗 | V（对抗） | ⚠️ 未系统采集 | 派生候选 + 人工确认 | **P0** |
| **多器具同框、仅一件在用** | 必须靠手交互挑出在用件 | V（对抗） | ⚠️ | 派生（同帧非手 bbox≥2） | **P0** |
| 在用器具被手完全遮挡 | 只能靠手运动模式推断 | V | ⚠️ | 派生（器具 bbox 消失但手在动） | P1 |
| 纯手动作、无特异器具 | idle/过渡靠手行为判 | V | ⚠️ | 派生（无特异器具 bbox） | P1 |

### Group B — 动作类别覆盖

| 等价类（动作） | 性质 | 现状（帧数 / 源视频） | 来源 | 优先级 |
|---|---|---|---|---|
| flush | V | ✅ 1109 | 派生（phase timeline） | 基线 |
| long_brush_insert | V | ✅ 1206 | 派生 | 基线 |
| long_brush_withdraw | V | ⚠️ 760 | 派生 | P1 |
| **air_injection** | V | ❌ **367，仅 1 源，val+test=0** | 派生 | **P0** |
| **short_brush_cleaning** | V | ❌ **252，2 源，val=0** | 派生 | **P0** |
| idle | V | ✅ ~22%（见 Group D） | 派生 | 基线 |

### Group C — 时序结构（分割特有）

**D-边界类型**（分割核心，直接决定边界误差）
| 等价类 | 判别依据 | 性质 | 现状 | 来源 | 优先级 |
|---|---|---|---|---|---|
| 清晰边界（换器械/明显切换） | 视觉+交互突变 | V | ✅ | 派生（相邻段） | 基线 |
| **模糊渐变边界** | 器具不变、动作性质渐变 | V | ⚠️ | 相邻段自动 + 少量手标 | **P0**（边界飘主因） |
| 瞬时过渡 <0.5s | 抖动易误切 | I | ⚠️ | 派生（段间隔） | P1 |
| 边界含 idle 停顿 | 操作者中途停顿 | V | ⚠️ | 派生 | P1 |

**D-段时长**
| 等价类 | 失败模式 | 性质 | 现状 | 来源 | 优先级 |
|---|---|---|---|---|---|
| 正常段 | — | V | ✅ | 派生（段长） | 基线 |
| 极短段 | 被吞并/漏识 | V | ⚠️ | 派生 | P1 |
| 超长段 | 中间被过分割 | V | ⚠️ | 派生 | P1 |
| 长短悬殊相邻 | 短段被长段淹没 | V | ⚠️ | 派生 | P1 |

**D-序列结构 / 转移**
| 等价类 | 考察点 | 性质 | 现状 | 来源 | 优先级 |
|---|---|---|---|---|---|
| 标准清洗顺序 | 基线 | V | ✅ | 派生（phase 序列） | 基线 |
| 动作重复（flush 多次 / insert-withdraw 反复） | 顺序鲁棒性 | V | ⚠️ | 派生 | P1 |
| 缺失 phase（跳过某动作） | 顺序鲁棒性 | V | ⚠️ | 派生 | P1 |
| **罕见转移对（训练未见 A→B）** | 是否过度依赖转移先验 | V | ❌ | 派生 + 选片 | **P0** |

**D-时序歧义**（外观相同，只能靠运动/时序区分）
| 等价类 | 判别只能靠 | 性质 | 现状 | 来源 | 优先级 |
|---|---|---|---|---|---|
| **insert vs withdraw** | 运动**方向** | V | ⚠️ | 派生（手/器具轨迹方向） | **P0** |
| 短刷清洗 vs 只是握着移动 | 运动**节律**（往复 vs 单调） | V | ⚠️ | 派生（轨迹周期性） | P1 |
| 无停顿衔接（withdraw 直接接 insert） | 时序上下文 | V | ⚠️ | 派生 | P1 |

### Group D — 鲁棒性诱因

**D-过分割诱因**（段内噪声，不应断段却常被断）
| 等价类 | 性质 | 现状 | 来源 | 优先级 |
|---|---|---|---|---|
| **段内短暂遮挡 / 手离场** | I | ⚠️ | 派生（手 bbox 短暂消失） | **P0** |
| 段内动作停顿 | I | ⚠️ | 派生（轨迹静止） | P1 |
| 段内视觉突变（反光闪 / 镜头晃） | I | ⚠️ | 手标 ec_env | P1 |

**D-idle / 负段 / 异常**
| 等价类 | 性质 | 现状 | 来源 | 优先级 |
|---|---|---|---|---|
| 段间 idle / 首尾 idle | V | ✅ | 派生（timeline gap） | 基线 |
| 长 idle（无操作） | V | ⚠️ | 派生 | P1 |
| 异常/未定义动作（器械掉落 → 判 idle/告警） | I | ❌ | 手标 ec_env（abnormal_action） | P2 |

### Group E — 来源多样性（泛化乘子）

> 状态见 §1「评测能力边界」。此处仅列等价类定义。

| 等价类 | 性质 | 现状 | v0.1 | 来源 |
|---|---|---|---|---|
| 同型号 seen | V | ✅ | 评测基线 | 源级 |
| unseen clip（整源留出） | V | ✅ 可做 | **评测**（尽量取不同录制场次） | 源级（视频级留出） |
| 异机位/角度 | V | ❌ 全缺 | 🟡 若可重录 | 源级（重录一小段） |
| 异内镜型号 | V | ❌ 全缺 | ⛔ 盲区 | 源级（需补录，暂无源） |
| 异操作者 | V | ❌ 全缺 | ⛔ 盲区 | 源级（需补录，暂无源） |

---

## 3. 数据增强可构造性评估

> **红线：增强样本原则上不进 benchmark test。** 本节结论用于 train 侧补覆盖，benchmark test 仍需真实策展。
> **时序分割几乎省不掉采集**——它的判据是交互、运动、时序结构、来源，全是语义/行为，增强伪造不了。可增强的仅限"往真实序列注入噪声"以造**过分割鲁棒性**探针，且会改变真实性，须谨慎。

判定档位：✅ 忠实增强｜⚠️ 近似（改变真实性/有风险）｜❌ 必真实采集。

| EC | 构造方式 | 说明 |
|---|---|---|
| 段内视觉突变（反光闪/晃动） | ✅ | 往真实序列注入合成闪光/抖动，构造过分割诱因 |
| 段内短暂遮挡 | ⚠️ | 对序列某几帧 random-erasing |
| 段时长（极短/超长） | ⚠️ | 时间重采样（加速/减速/截断），改变运动速度真实性 |
| 瞬时过渡 | ⚠️ | 时间压缩相邻段 |
| 对抗-可见性（在场未操作/多器具同框） | ❌ | 无法伪造"器具在场但手不操作"的真实交互 |
| insert vs withdraw / 相似动作 | ❌ | 真实运动方向/节律 |
| 罕见转移对/动作重复/缺失 phase | ❌ | 真实流程结构 |
| air_injection / short_brush_cleaning 类别 | ❌ | 真实动作 |
| 模糊渐变边界 | ❌ | 真实 |
| 长 idle / 异常动作 | ❌ | 真实（异常可摆拍） |
| 异机位/角度 | ❌ | 必真实重录 |
| 异内镜型号 / 异操作者 | ❌❌ | 增强造不出，且暂无源（见 §1 盲区） |

**小结**：时序分割的核心 EC 全部需真实采集；增强只在"过分割鲁棒性"上有限可用，且仅进 train。

---

## 4. 优先级汇总

> 泛化相关项区分"可执行"与"阻塞"：跨型号/操作者见 §1，暂不计入 v0.1 可执行 P0。

**P0（现有数据可执行的必测核心）**
- **对抗-可见性**：器具在场但未操作、多器具同框仅一件在用 ← 本套最核心，防捷径
- 相似动作混淆：insert vs withdraw（纯靠运动方向）
- 类别 + split 缺口：`air_injection`（1 源，val+test=0）、`short_brush_cleaning`（2 源，val=0）
- 模糊渐变边界（边界定位）
- 罕见转移对（转移先验依赖）
- 段内遮挡 / 手离场（过分割头号诱因）
- unseen clip（整源留出，尽量取不同录制场次）

**P0（阻塞，待数据）**
- 异内镜型号泛化 ⛔（见 §1，暂不计入可执行 P0）

**P1（重要难点）**
- `long_brush_withdraw`
- 在用器具被遮挡靠运动推断、纯手动作段
- 瞬时过渡、边界含 idle 停顿
- 极短段、超长段、长短悬殊相邻
- 动作重复、缺失 phase
- 短刷清洗 vs 握着移动、无停顿衔接
- 段内停顿、段内视觉突变
- 长 idle
- 异机位/角度（🟡 若可重录）；异操作者 ⛔（阻塞）

**P2 / 基线**
- flush、long_brush_insert（已饱和，作对照基线）
- 清晰边界、标准顺序、正常段、器具在用
- 段间/首尾 idle
- 异常/未定义动作

---

## 5. 对抗-可见性用例（专门强调）

benchmark test 必须包含以下"detection 单看会判错"的序列，否则捷径模型会虚高：

| 用例 | 场景 | 正确判定 | 陷阱 |
|---|---|---|---|
| 在场未操作·气枪 | flush/idle 段，气枪躺在台上同框 | 非 air_injection | 看到 air_gun 就报注气 |
| 在场未操作·注射器 | air_injection 段，注射器杂物同框 | 非 flush | 看到 syringe 就报冲洗 |
| 多器具同框 | 多件小器械同时可见，仅一件在手操作 | 按在用件判 | 按"可见类别多数"投票 |
| 器具被手全遮挡 | 在用器具被手挡住 | 靠手运动维持当前动作 | 器具 bbox 消失就断段/改判 |

> 判分：这些序列上报**边界误差 + 误判段占比**，并单列"对抗-可见性子集"的 F1，与普通子集对比衰减。

---

## 6. 手标 vs 自动派生（策展分工）

- **自动派生（核心，从手-器具几何 + 轨迹算）**：
  - 交互状态（器具 bbox 与 hand 的 IoU/距离 + 是否共动）→ 在用 vs 在场
  - 多器具同框（同帧非手 bbox 数）
  - 手运动模式（hand bbox 轨迹方向/往复周期）→ insert/withdraw/清洗
  - 边界/时长/顺序/转移/idle gap（全从 phase timeline 结构算）
  - 段内遮挡（手 bbox 短暂消失）
  > 这是 ActionMixed「bbox + 动作同存」设计的价值：模型和派生脚本都能用手-器具相对几何，而不是只看类别是否出现。
- **手标（时间窗口，一段一段）**：段内视觉突变（反光/晃动）、异常动作 → LS benchmark 项目的 `ec_env` TimelineLabels。
- **人工确认**：对抗-可见性的 P0 用例（在场未操作、多器具同框）先自动出候选，再人工核对是否确实"未操作"。
- **源级（选片时定）**：来源多样性（型号/操作者/机位）。

---

## 7. test 集策展与指标切片

1. **源级隔离**：test 视频整条从 train/val 排除，写入 `benchmark_test.yaml`；ActionMixed 现有"段级 hash 切分"对 benchmark **禁用**（同视频跨 split = 转移先验泄漏）。
2. **整段保留**：每条 test 是完整序列，含动作前后上下文与边界，绝不截散帧。
3. **每序列/每边界打 EC 标签** → 覆盖矩阵，对照本文 P0/P1 核对，并标出 §1 盲区未覆盖项。
4. **指标切片**：全局 edit/F1@τ 之外，按 EC 桶切片——
   - 对抗-可见性子集 vs 普通子集的 F1 衰减
   - 模糊边界 vs 清晰边界的边界定位误差
   - 极短段 recall、超长段的段数比
   - insert/withdraw 混淆矩阵
   - unseen clip（不同场次）vs seen 的各指标衰减
5. **冻结版本**：定版打 tag（如 `benchmark-seg-v0.1`），只增不改；盲区项在版本说明里显式列出。
